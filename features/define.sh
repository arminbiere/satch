# Automatically generated by 'features/generate'.

# Compiler definitions to disable features.

[ $active = no ] && CFLAGS="$CFLAGS -DNACTIVE"
[ $best = no ] && CFLAGS="$CFLAGS -DNBEST"
[ $block = no ] && CFLAGS="$CFLAGS -DNBLOCK"
[ $bump = no ] && CFLAGS="$CFLAGS -DNBUMP"
[ $cache = no ] && CFLAGS="$CFLAGS -DNCACHE"
[ $color = no ] && CFLAGS="$CFLAGS -DNCOLOR"
[ $focused = no ] && CFLAGS="$CFLAGS -DNFOCUSED"
[ $glue = no ] && CFLAGS="$CFLAGS -DNGLUE"
[ $inverted = no ] && CFLAGS="$CFLAGS -DNINVERTED"
[ $learn = no ] && CFLAGS="$CFLAGS -DNLEARN"
[ $minimize = no ] && CFLAGS="$CFLAGS -DNMINIMIZE"
[ $reasons = no ] && CFLAGS="$CFLAGS -DNREASONS"
[ $reduce = no ] && CFLAGS="$CFLAGS -DNREDUCE"
[ $rephase = no ] && CFLAGS="$CFLAGS -DNREPHASE"
[ $restart = no ] && CFLAGS="$CFLAGS -DNRESTART"
[ $reuse = no ] && CFLAGS="$CFLAGS -DNREUSE"
[ $rsort = no ] && CFLAGS="$CFLAGS -DNRSORT"
[ $save = no ] && CFLAGS="$CFLAGS -DNSAVE"
[ $sort = no ] && CFLAGS="$CFLAGS -DNSORT"
[ $stable = no ] && CFLAGS="$CFLAGS -DNSTABLE"
[ $target = no ] && CFLAGS="$CFLAGS -DNTARGET"
[ $tier1 = no ] && CFLAGS="$CFLAGS -DNTIER1"
[ $tier2 = no ] && CFLAGS="$CFLAGS -DNTIER2"
[ $true = no ] && CFLAGS="$CFLAGS -DNTRUE"
[ $used = no ] && CFLAGS="$CFLAGS -DNUSED"
[ $variadic = no ] && CFLAGS="$CFLAGS -DNVARIADIC"
[ $virtual = no ] && CFLAGS="$CFLAGS -DNVIRTUAL"
[ $vmtf = no ] && CFLAGS="$CFLAGS -DNVMTF"
[ $vsids = no ] && CFLAGS="$CFLAGS -DNVSIDS"
